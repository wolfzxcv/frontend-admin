import { I18nMessage } from '../@types'
import { common, components, main, notify, sweetalert } from './index'

const en: I18nMessage = {
  ...common,
  components,
  main,
  notify,
  sweetalert,
}

export default en
