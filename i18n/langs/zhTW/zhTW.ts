import { I18nMessage } from '../@types'
import {
  common,
  components,
  main,
  notify,
  register,
  reservation,
  sweetalert,
} from './index'

const zhTW: I18nMessage = {
  ...common,
  components,
  main,
  notify,
  register,
  reservation,
  sweetalert,
}

export default zhTW
